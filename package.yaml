name: lib
version: 0.1.0.0
dependencies:
  - base == 4.*
  - hourglass
  - aeson
  - yaml
  - bytestring
  - text
  - filepath
  - optparse-applicative
  - process
  - process-extras
executables:
  ddgram:
    main: Main.hs
    source-dirs:  app
    dependencies: lib
library:
  source-dirs:   src
